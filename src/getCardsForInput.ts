/**
 * @fileoverview get the card information for the supplied input
 */
import { CardType } from "./types";
//export function buildCardTrie()
export const getCardsForInput = (input: string): CardType[] => {
    //TODO!

    return [];
};